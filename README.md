# Item-Highlighter
Item Highlighter helps users quickly locate and highlight features across multiple layers using attribute values. Instead of manually searching through attribute tables and navigating between layers, users can select predefined values from simple dropdown lists.
